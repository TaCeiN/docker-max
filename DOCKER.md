# Полная инструкция по деплою через Docker

Этот документ содержит **пошаговую инструкцию** по деплою проекта с момента клонирования репозитория до полного запуска всех сервисов.

**Предполагается, что:**
- ✅ Docker и Docker Compose уже установлены на вашем сервере
- ✅ У вас есть VPS с публичным IP адресом
- ✅ У вас есть домен (или поддомены) для backend и webhook

---

## 📋 Содержание

1. [Клонирование репозитория](#1-клонирование-репозитория)
2. [Подготовка переменных окружения](#2-подготовка-переменных-окружения)
3. [Настройка DNS записей](#3-настройка-dns-записей)
4. [Настройка Firewall](#4-настройка-firewall)
5. [Выбор варианта деплоя](#5-выбор-варианта-деплоя)
6. [Запуск деплоя](#6-запуск-деплоя)
7. [Проверка работы](#7-проверка-работы)
8. [Управление сервисами](#8-управление-сервисами)
9. [Устранение проблем](#9-устранение-проблем)

---

## 1. Клонирование репозитория

### Шаг 1.1: Подключитесь к вашему VPS

```bash
# Подключитесь к серверу через SSH
ssh user@your-vps-ip
```

### Шаг 1.2: Клонируйте репозиторий

```bash
# Перейдите в нужную директорию (например, /opt или /home/user)
cd /opt

# Клонируйте репозиторий
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> docker-max-main

# Перейдите в директорию проекта
cd docker-max-main
```

**Пример:**
```bash
cd /opt
git clone https://github.com/your-username/docker-max-main.git
cd docker-max-main
```

### Шаг 1.3: Проверьте структуру проекта

```bash
# Убедитесь, что все файлы на месте
ls -la

# Должны быть видны:
# - backend/
# - frontend/
# - docker-compose.manual.yml
# - docker-compose.github.yml
# - env.example
# - deploy-github.sh
# - deploy-manual.sh
```

---

## 2. Подготовка переменных окружения

### Шаг 2.1: Создайте файл .env

```bash
# Скопируйте пример файла
cp env.example .env

# Откройте файл для редактирования
nano .env
# или
vim .env
```

### Шаг 2.2: Получите токен бота Max Bot

1. Откройте Telegram и найдите бота: [@MasterBot](https://tt.me/MasterBot)
2. Создайте бота или получите токен существующего бота
3. Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2.3: Сгенерируйте SECRET_KEY

**Вариант 1: Python (если установлен)**
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

**Вариант 2: OpenSSL (рекомендуется)**
```bash
openssl rand -base64 32
```

**Вариант 3: /dev/urandom (Linux)**
```bash
cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1
```

**Скопируйте сгенерированный ключ** - он понадобится в следующем шаге.

### Шаг 2.4: Заполните обязательные переменные в .env

Откройте файл `.env` и заполните следующие переменные:

#### Обязательные для всех вариантов:

```env
# Токен бота (получите от @MasterBot)
MAX_BOT_TOKEN=ваш_токен_бота_здесь

# Домены (замените на ваши)
BACKEND_DOMAIN=backend-devcore-max.cloudpub.ru
WEBHOOK_DOMAIN=webhook-devcore-max.cloudpub.ru

# URL (должны начинаться с https://)
BACKEND_URL=https://backend-devcore-max.cloudpub.ru
WEBHOOK_URL=https://webhook-devcore-max.cloudpub.ru

# Секретный ключ (сгенерируйте как описано выше)
SECRET_KEY=ваш_сгенерированный_ключ_здесь

# Email для Let's Encrypt (для получения SSL сертификатов)
LETSENCRYPT_EMAIL=your-email@example.com
```

#### Дополнительные настройки (опционально):

```env
# Время до удаления уведомления после прочтения (в секундах)
NOTIFICATION_DELETE_AFTER_READ_SECONDS=43200

# URL изображения для уведомлений (опционально)
NOTIFICATION_IMAGE_URL=https://example.com/image.png

# Срок действия JWT токена (в минутах)
ACCESS_TOKEN_EXPIRE_MINUTES=180
```

#### Только для варианта 1 (GitHub Pages):

```env
# GitHub Personal Access Token
GITHUB_TOKEN=ваш_github_token_здесь

# Репозиторий GitHub Pages
GITHUB_REPO=username/repository

# GitHub пользователь
GITHUB_USER=username

# GitHub email
GITHUB_EMAIL=your-email@example.com
```

### Шаг 2.5: Создание GitHub Personal Access Token (только для варианта 1)

Если вы выбрали вариант 1 (деплой на GitHub Pages):

1. Перейдите на GitHub: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Назовите токен (например: "GitHub Pages Deploy")
4. Выберите срок действия:
   - **"No expiration"** (рекомендуется для production)
   - Или **"90 days"** (для тестирования)
5. Выберите права доступа:
   - ✅ **`repo`** (полный доступ к репозиториям)
   - Или минимум: **`public_repo`** (если репозиторий публичный)
6. Нажмите **"Generate token"**
7. **Скопируйте токен сразу!** (он больше не будет показан)
8. Вставьте токен в `.env` файл в переменную `GITHUB_TOKEN`

### Шаг 2.6: Сохраните файл .env

```bash
# Если использовали nano:
# Нажмите Ctrl+O для сохранения, Enter для подтверждения, Ctrl+X для выхода

# Если использовали vim:
# Нажмите Esc, затем :wq и Enter для сохранения и выхода
```

### Шаг 2.7: Проверьте, что .env не попадёт в git

```bash
# Убедитесь, что .env в .gitignore
grep -q "^\.env$" .gitignore && echo "✅ .env уже в .gitignore" || echo ".env" >> .gitignore
```

---

## 3. Настройка DNS записей

### Шаг 3.1: Узнайте IP адрес вашего VPS

```bash
# На вашем VPS выполните:
curl ifconfig.me
# или
hostname -I
```

**Скопируйте IP адрес** - он понадобится для настройки DNS.

### Шаг 3.2: Настройте DNS записи в панели управления доменом

1. Войдите в панель управления вашим доменом (где вы покупали домен)
2. Найдите раздел **"DNS Management"** или **"Управление DNS"**
3. Добавьте **A-записи**:

   | Тип | Имя (Host) | Значение (Value) | TTL |
   |-----|------------|------------------|-----|
   | A   | backend-devcore-max | IP_ВАШЕГО_VPS | 3600 |
   | A   | webhook-devcore-max | IP_ВАШЕГО_VPS | 3600 |

   **Пример:**
   - Если ваш домен: `cloudpub.ru`
   - И IP VPS: `123.45.67.89`
   - То записи будут:
     - `backend-devcore-max.cloudpub.ru` → `123.45.67.89`
     - `webhook-devcore-max.cloudpub.ru` → `123.45.67.89`

4. **Сохраните изменения**

### Шаг 3.3: Дождитесь распространения DNS

```bash
# Проверьте, что DNS записи резолвятся
nslookup backend-devcore-max.cloudpub.ru
nslookup webhook-devcore-max.cloudpub.ru

# Или используйте dig (если установлен)
dig backend-devcore-max.cloudpub.ru +short
dig webhook-devcore-max.cloudpub.ru +short
```

**Важно:** 
- DNS записи могут распространяться от нескольких минут до 24 часов
- Не запускайте деплой, пока DNS записи не резолвятся на правильный IP
- Без настроенных DNS записей SSL сертификаты не будут получены!

---

## 4. Настройка Firewall

### Шаг 4.1: Автоматическая настройка (рекомендуется)

```bash
# Сделайте скрипт исполняемым
chmod +x setup-firewall.sh

# Запустите скрипт (требует sudo)
sudo ./setup-firewall.sh
```

Скрипт автоматически:
- Определит тип firewall (UFW, firewalld, или iptables)
- Откроет порты 80 (HTTP) и 443 (HTTPS)
- Настроит правила firewall

### Шаг 4.2: Ручная настройка (если автоматическая не работает)

#### UFW (Ubuntu/Debian):

```bash
# Откройте порты
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp  # SSH (если еще не открыт)

# Проверьте статус
sudo ufw status

# Если firewall не активен, включите его
sudo ufw enable
```

#### firewalld (CentOS/RHEL):

```bash
# Откройте порты
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload

# Проверьте статус
sudo firewall-cmd --list-all
```

#### iptables (если используется напрямую):

```bash
# Откройте порты
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT

# Сохраните правила (зависит от дистрибутива)
# Ubuntu/Debian:
sudo iptables-save > /etc/iptables/rules.v4

# CentOS/RHEL:
sudo service iptables save
```

---

## 5. Выбор варианта деплоя

Проект поддерживает **два варианта** деплоя фронтенда:

### Вариант 1: GitHub Pages (автоматический деплой)

**Преимущества:**
- ✅ Автоматический деплой фронтенда
- ✅ Бесплатный хостинг на GitHub Pages
- ✅ Автоматическое обновление при каждом деплое

**Требования:**
- GitHub аккаунт
- GitHub Personal Access Token (создан в шаге 2.5)
- Репозиторий для GitHub Pages

**Используйте:** `docker-compose.github.yml`

### Вариант 2: Ручной деплой

**Преимущества:**
- ✅ Полный контроль над деплоем
- ✅ Можно использовать любой хостинг
- ✅ Не требует GitHub токена

**Требования:**
- Собственный хостинг для фронтенда
- Возможность загружать файлы на хостинг

**Используйте:** `docker-compose.manual.yml`

---

## 6. Запуск деплоя

### Вариант 1: Деплой на GitHub Pages

#### Шаг 6.1.1: Сделайте скрипт исполняемым

```bash
chmod +x deploy-github.sh
```

#### Шаг 6.1.2: Запустите деплой

```bash
./deploy-github.sh
```

**Что происходит:**
1. ✅ Проверка наличия `.env` файла
2. ✅ Проверка всех обязательных переменных
3. ✅ Запуск nginx-proxy и letsencrypt контейнеров
4. ✅ Сборка и запуск backend и webhook контейнеров
5. ✅ Сборка фронтенда с правильными адресами
6. ✅ Автоматический деплой фронтенда на GitHub Pages
7. ✅ Автоматическая подписка на webhooks
8. ✅ Получение SSL сертификатов (может занять несколько минут)

**Время выполнения:** 5-15 минут (в зависимости от скорости интернета и сервера)

#### Шаг 6.1.3: Следите за логами

Скрипт покажет статус каждого этапа. Если что-то пойдёт не так, вы увидите ошибку.

---

### Вариант 2: Ручной деплой

#### Шаг 6.2.1: Сделайте скрипт исполняемым

```bash
chmod +x deploy-manual.sh
```

#### Шаг 6.2.2: Запустите сборку и деплой

```bash
./deploy-manual.sh
```

**Что происходит:**
1. ✅ Проверка наличия `.env` файла
2. ✅ Проверка всех обязательных переменных
3. ✅ Сборка фронтенда с правильными адресами
4. ✅ Сохранение собранных файлов в Docker volume `frontend-build`
5. ✅ Запуск nginx-proxy и letsencrypt контейнеров
6. ✅ Сборка и запуск backend и webhook контейнеров
7. ✅ Автоматическая подписка на webhooks
8. ✅ Получение SSL сертификатов (может занять несколько минут)

#### Шаг 6.2.3: Скопируйте файлы фронтенда из volume

После успешной сборки скопируйте файлы для ручного деплоя:

```bash
# Создайте директорию для файлов
mkdir -p frontend-dist

# Скопируйте файлы из Docker volume
docker run --rm \
  -v unitask_frontend-build:/source \
  -v $(pwd)/frontend-dist:/dest \
  alpine sh -c 'cp -r /source/* /dest/'

# Проверьте, что файлы скопированы
ls -la frontend-dist/
```

**Теперь загрузите содержимое `frontend-dist/` на ваш хостинг:**
- Через FTP/SFTP
- Через панель управления хостингом
- Через любой другой способ загрузки файлов

---

## 7. Проверка работы

### Шаг 7.1: Проверьте статус контейнеров

```bash
# Вариант 1 (GitHub Pages)
docker compose -f docker-compose.github.yml ps

# Вариант 2 (Ручной деплой)
docker compose -f docker-compose.manual.yml ps
```

**Все контейнеры должны быть в статусе "Up" или "running":**
- `nginx-proxy` - Up
- `nginx-letsencrypt` - Up
- `unitask-backend` - Up
- `unitask-webhook` - Up
- `unitask-frontend-deploy` (только вариант 1) - Exited (0) - это нормально

### Шаг 7.2: Проверьте логи

```bash
# Просмотр всех логов
docker compose -f docker-compose.github.yml logs -f

# Или логов конкретного сервиса
docker compose -f docker-compose.github.yml logs backend
docker compose -f docker-compose.github.yml logs webhook
docker compose -f docker-compose.github.yml logs letsencrypt
```

**Нажмите Ctrl+C для выхода из просмотра логов.**

### Шаг 7.3: Проверьте health endpoints

```bash
# Проверка backend
curl https://backend-devcore-max.cloudpub.ru/health

# Проверка webhook
curl https://webhook-devcore-max.cloudpub.ru/health
```

**Ожидаемый ответ:** `{"status":"ok"}` или `{"status":"healthy"}`

### Шаг 7.4: Проверьте SSL сертификаты

```bash
# Проверка сертификата backend
openssl s_client -connect backend-devcore-max.cloudpub.ru:443 -servername backend-devcore-max.cloudpub.ru < /dev/null 2>/dev/null | openssl x509 -noout -dates

# Проверка сертификата webhook
openssl s_client -connect webhook-devcore-max.cloudpub.ru:443 -servername webhook-devcore-max.cloudpub.ru < /dev/null 2>/dev/null | openssl x509 -noout -dates
```

**Или просто откройте в браузере:**
- https://backend-devcore-max.cloudpub.ru/health
- https://webhook-devcore-max.cloudpub.ru/health

**Проверьте:**
- ✅ В адресной строке должен быть замок 🔒
- ✅ Сертификат должен быть валидным
- ✅ Не должно быть предупреждений о безопасности

**Примечание:** Первое получение SSL сертификатов может занять 5-10 минут. Если сертификаты ещё не получены, проверьте логи letsencrypt:

```bash
docker compose -f docker-compose.github.yml logs letsencrypt
```

### Шаг 7.5: Проверьте фронтенд

**Вариант 1 (GitHub Pages):**
- Откройте в браузере: `https://username.github.io`
- Фронтенд должен загрузиться и работать

**Вариант 2 (Ручной деплой):**
- Откройте URL вашего хостинга, где вы загрузили файлы
- Фронтенд должен загрузиться и работать

### Шаг 7.6: Проверьте работу мини-приложения

1. Откройте мини-приложение в Max боте
2. Попробуйте войти в аккаунт
3. Проверьте, что данные загружаются
4. Проверьте основные функции

---

## 8. Управление сервисами

### Просмотр статуса

```bash
# Вариант 1
docker compose -f docker-compose.github.yml ps

# Вариант 2
docker compose -f docker-compose.manual.yml ps
```

### Просмотр логов

```bash
# Все логи
docker compose -f docker-compose.github.yml logs -f

# Конкретный сервис
docker compose -f docker-compose.github.yml logs -f backend
docker compose -f docker-compose.github.yml logs -f webhook
```

### Перезапуск сервисов

```bash
# Перезапуск всех сервисов
docker compose -f docker-compose.github.yml restart

# Перезапуск конкретного сервиса
docker compose -f docker-compose.github.yml restart backend
```

### Остановка сервисов

```bash
# Остановка всех сервисов (без удаления)
docker compose -f docker-compose.github.yml stop

# Остановка с удалением контейнеров
docker compose -f docker-compose.github.yml down
```

### Полная остановка с удалением данных

⚠️ **ВНИМАНИЕ:** Это удалит базу данных и все данные!

```bash
# Остановка с удалением контейнеров и volumes
docker compose -f docker-compose.github.yml down -v
```

### Обновление кода

```bash
# 1. Остановите сервисы
docker compose -f docker-compose.github.yml down

# 2. Обновите код из репозитория
git pull

# 3. Пересоберите и запустите заново
./deploy-github.sh
# или
./deploy-manual.sh
```

---

## 9. Устранение проблем

### Проблема: "MAX_BOT_TOKEN is not set"

**Решение:**
```bash
# Проверьте, что .env файл существует
ls -la .env

# Проверьте, что переменная установлена
grep MAX_BOT_TOKEN .env

# Если переменной нет, добавьте её в .env
nano .env
```

### Проблема: "SECRET_KEY is not set"

**Решение:**
```bash
# Сгенерируйте новый ключ
openssl rand -base64 32

# Добавьте его в .env
nano .env
# Добавьте строку: SECRET_KEY=сгенерированный_ключ
```

### Проблема: "GITHUB_TOKEN is not set" (вариант 1)

**Решение:**
1. Создайте GitHub Personal Access Token (см. шаг 2.5)
2. Добавьте его в `.env` файл:
   ```bash
   nano .env
   # Добавьте: GITHUB_TOKEN=ваш_токен
   ```

### Проблема: SSL сертификаты не получены

**Проверьте:**

1. **DNS записи настроены:**
   ```bash
   nslookup backend-devcore-max.cloudpub.ru
   nslookup webhook-devcore-max.cloudpub.ru
   ```
   Должны резолвиться на IP вашего VPS.

2. **Порты открыты:**
   ```bash
   # Проверьте firewall
   sudo ufw status
   # или
   sudo firewall-cmd --list-all
   ```
   Порты 80 и 443 должны быть открыты.

3. **Логи letsencrypt:**
   ```bash
   docker compose -f docker-compose.github.yml logs letsencrypt
   ```
   Ищите ошибки в логах.

4. **Домены доступны из интернета:**
   ```bash
   # С другого компьютера проверьте
   curl -I http://backend-devcore-max.cloudpub.ru
   ```
   Должен вернуться ответ от сервера.

**Решение:**
- Убедитесь, что DNS записи настроены и резолвятся
- Проверьте, что порты 80 и 443 открыты в firewall
- Дождитесь распространения DNS (может занять до 24 часов)
- Проверьте, что в `.env` указан правильный `LETSENCRYPT_EMAIL`

### Проблема: "502 Bad Gateway" или "Connection refused"

**Проверьте:**

1. **Контейнеры запущены:**
   ```bash
   docker compose -f docker-compose.github.yml ps
   ```
   Все контейнеры должны быть в статусе "Up".

2. **Логи контейнеров:**
   ```bash
   docker compose -f docker-compose.github.yml logs backend
   docker compose -f docker-compose.github.yml logs webhook
   ```
   Ищите ошибки в логах.

3. **Контейнеры в одной сети:**
   ```bash
   docker network inspect unitask-network
   ```
   Все контейнеры должны быть в сети `unitask-network`.

**Решение:**
- Перезапустите контейнеры:
  ```bash
  docker compose -f docker-compose.github.yml restart
  ```
- Если не помогло, пересоберите:
  ```bash
  docker compose -f docker-compose.github.yml down
  ./deploy-github.sh
  ```

### Проблема: "Database locked"

**Решение:**
```bash
# Остановите все контейнеры
docker compose -f docker-compose.github.yml down

# Подождите несколько секунд
sleep 5

# Запустите заново
./deploy-github.sh
```

### Проблема: Фронтенд не подключается к backend

**Проверьте:**

1. **VITE_API_URL в собранном фронтенде:**
   ```bash
   # Для варианта 1 - проверьте в GitHub Pages
   # Для варианта 2 - проверьте в собранных файлах
   grep -r "backend-devcore-max" frontend-dist/
   ```

2. **CORS настройки:**
   - Backend должен разрешать запросы с домена фронтенда
   - Проверьте логи backend на наличие CORS ошибок

**Решение:**
- Убедитесь, что `BACKEND_URL` в `.env` правильный
- Пересоберите фронтенд:
  ```bash
  docker compose -f docker-compose.github.yml down
  ./deploy-github.sh
  ```

### Проблема: Webhook не работает

**Проверьте:**

1. **Webhook подписан:**
   ```bash
   docker compose -f docker-compose.github.yml logs webhook | grep -i "subscribe\|webhook"
   ```
   Должны быть сообщения об успешной подписке.

2. **WEBHOOK_URL доступен:**
   ```bash
   curl https://webhook-devcore-max.cloudpub.ru/health
   ```
   Должен вернуться `{"status":"ok"}`.

**Решение:**
- Проверьте, что `WEBHOOK_URL` в `.env` правильный
- Убедитесь, что webhook сервер запущен
- Проверьте логи webhook на наличие ошибок

---

## 📚 Дополнительная информация

### Архитектура проекта

Проект использует автоматическую настройку через Docker:

1. **nginx-proxy** - автоматически настраивает reverse proxy для всех контейнеров
2. **letsencrypt** - автоматически получает и обновляет SSL сертификаты
3. **backend** - FastAPI сервер на порту 8000
4. **webhook** - Webhook сервер на порту 8080
5. **frontend** - React приложение (деплоится на GitHub Pages или вручную)

### База данных

- Используется SQLite
- Файл БД: `data.sqlite3` в Docker volume `backend-data`
- Backend и webhook используют один общий файл БД
- SQLite поддерживает concurrent access

### Автоматическая подписка на webhooks

При старте webhook контейнера автоматически:
1. Проверяется наличие `MAX_BOT_TOKEN`
2. Проверяются текущие подписки
3. Удаляется старая подписка (если существует)
4. Создаётся новая подписка на `WEBHOOK_URL`
5. Запускается webhook сервер

### Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` в git
- Не показывайте токены в логах или консоли
- Используйте токены с минимальными необходимыми правами
- Регулярно обновляйте токены (рекомендуется каждые 90 дней)
- Используйте уникальный `SECRET_KEY` для каждого окружения

---

## ✅ Чек-лист перед деплоем

Перед запуском деплоя убедитесь, что:

- [ ] Репозиторий клонирован
- [ ] Файл `.env` создан и заполнен
- [ ] `MAX_BOT_TOKEN` получен и добавлен в `.env`
- [ ] `SECRET_KEY` сгенерирован и добавлен в `.env`
- [ ] DNS записи настроены и резолвятся
- [ ] Порты 80 и 443 открыты в firewall
- [ ] Для варианта 1: GitHub токен создан и добавлен в `.env`
- [ ] Все домены в `.env` указаны правильно
- [ ] `LETSENCRYPT_EMAIL` указан правильно

---

## 🎉 Готово!

После успешного деплоя ваше приложение будет доступно:

- **Backend API:** `https://backend-devcore-max.cloudpub.ru`
- **Webhook:** `https://webhook-devcore-max.cloudpub.ru`
- **Frontend:** `https://username.github.io` (вариант 1) или ваш хостинг (вариант 2)

Все сервисы будут автоматически перезапускаться при перезагрузке сервера благодаря `restart: unless-stopped`.

SSL сертификаты будут автоматически обновляться до истечения срока действия.

---

## 📞 Поддержка

Если у вас возникли проблемы, которые не описаны в этом документе:

1. Проверьте логи контейнеров
2. Проверьте раздел "Устранение проблем"
3. Убедитесь, что все шаги выполнены правильно
4. Проверьте документацию Docker и используемых сервисов
